#include "holberton.h"
/**
 *_isupper - checks uppercase
 *@c: Variable
 * Return: 1 is uppercase 0 otherwise
 */
int _isupper(int c)
{
if (c > 'A' && c < 'Z')
{
return (1);
}
else if (c > 'a' && c < 'z')
{
}
return (0);
}
