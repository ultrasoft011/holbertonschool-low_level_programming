#include "holberton.h"
/**
 *print_array - prints an array
 *@a: Pointer
 *@n: Variable
 */

void print_array(int *a, int n)
{
int counter = 0;
int tmp;

while (a[counter] != '\0')
{
tmp = n[counter];
a = tmp;
counter++
}

while (j != '\0')
{
printf("%d", a[j]
}
_putchar('\n');
}
